apiVersion: v1
kind: Service
metadata:
  name: {{ include "cx-argo-cd-example.fullname" . }}
  labels:
    {{- include "cx-argo-cd-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cx-argo-cd-example.selectorLabels" . | nindent 4 }}
